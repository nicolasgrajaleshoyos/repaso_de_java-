package ciclos;

public class random {

        /*


        int numeror;
        int numeroIn = 0;
        int contador = 0;


        numeror = (int) (Math.random() * 100);


        while (numeroIn != numeror) {
            numeroIn = Integer.parseInt(JOptionPane.showInputDialog(null, "Ingrese un número (0-99):"));
            contador++;


            if (numeroIn == numeror) {
                JOptionPane.showMessageDialog(null, "numero corrcto el numero es : " + numeror +"los intentos fueron de "+contador);
            } else if (numeror > numeroIn) {
                JOptionPane.showMessageDialog(null, "el numero  es mayor ");

            } else if (numeror < numeroIn) {
                JOptionPane.showMessageDialog(null, "el numero  es menor ");


            }

        }


        int numero, suma = 0;
        do {
            numero = Integer.parseInt(JOptionPane.showInputDialog("ingresa un numero"));
            suma += numero;//toma el valor actual de suma, suma numero a ese valor y luego asigna el resultado de nuevo a suma
        } while (numero != 0);

        System.out.println("el resultado de la suma es: " + suma);

    }
   */
}

import java.util.Scanner;

public class parOimpar {

        Scanner sc = new Scanner(System.in);


        /*
         //aca se esta verificando si es par por el residuo de la divicion
           int numero;


        numero = Integer.parseInt(JOptionPane.showInputDialog(null, "diguite un numero"));

        while (numero != 0) {
            if (numero % 2 == 0) {
                JOptionPane.showMessageDialog(null, "El numero es: " + "par");

            } else {
                JOptionPane.showMessageDialog(null, "El numero es: " + "impar");
            }
            numero = Integer.parseInt(JOptionPane.showInputDialog(null, "diguite un numero"));
        }



        int numero, cont = 0;

        numero = Integer.parseInt(JOptionPane.showInputDialog("Ingrese un numero: "));


        while (numero >= 0) {
            JOptionPane.showMessageDialog(null, "Ha ingresado el numero: " + numero);
            cont++;

            numero = Integer.parseInt(JOptionPane.showInputDialog("Ingrese otro numero: "));
        }
        JOptionPane.showMessageDialog(null, "Numero no válido. Ingresó un total de " + cont + " numeros.");
    }
    */

    }

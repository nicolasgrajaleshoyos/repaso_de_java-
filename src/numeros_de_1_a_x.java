import javax.swing.*;
import java.util.Scanner;

public class numeros_de_1_a_x {
        Scanner sc = new Scanner(System.in);

        /*
          int numero;

        numero=Integer.parseInt(JOptionPane.showInputDialog(null,"diguite el numero "));

        for (int i = 1; i <= numero ; i++) {
            System.out.println("los numeros son " +i);
        }
         */
      /*
        int num, suma=0;
        // aqui la condición te respeta el ingreso de 10 dígitos. Un'i<=10'te pide ingresar 11 digitos.
        for(int i=0; i<10; i++){
            num = Integer.parseInt(JOptionPane.showInputDialog("Ingrese un número"));
            System.out.println(num);
            suma+=num;  //suma iterativa
        }
        System.out.println("La suma total de los números ingresados es: "+suma);
    }
    }

       */
    }
